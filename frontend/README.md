# ScoreGenius – Frontend

This folder contains the React + Vite PWA.

- **Primary documentation lives at the repo root**: see `../README.md`.
- Quickstart:
  ```bash
  nvm use
  npm ci
  npm run dev
